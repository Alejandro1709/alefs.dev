---
import type { ArticleType } from '../types/article';

export interface ArticlePageProps {
  article: ArticleType;
}

const { article } = Astro.props;
---

<article
  class='h-[200px] bg-slate-400 hover:scale-95 transition-all cursor-pointer hover:shadow-md'
>
  {article.type}
</article>
